# Applicable Standards

No formal standards index exists yet (`agent-os/standards/` not present).

## Conventions followed from CLAUDE.md
- Max 300 lines per file
- No inline secrets (env vars only)
- All API routes require OpenAPI annotations
- Backend changes owned by backend agent
- Frontend changes owned by frontend agent
- DB changes owned by db-admin agent
